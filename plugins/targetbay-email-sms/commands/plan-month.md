---
description: Plan a month of email and SMS marketing for this TargetBay Email & SMS store
argument-hint: [month, e.g. "next month" or "December"]
---

Use the `monthly-marketing-planner` skill to plan $1.

Read the store's own history through the TargetBay Email & SMS MCP before proposing anything. Establish cadence
capacity first, then derive how many campaigns the period supports — the campaign count is an output,
never an input, and an empty week is a valid answer.

Return the dated calendar the skill specifies: date, campaign, objective, audience, channel, product,
offer, content direction, expected outcome, dependencies and risk.

Plan only. Create nothing, schedule nothing and send nothing without explicit human approval
(`rules/safety-rules.md`). If a required capability is missing, report `blocked` with the unmet
requirement — do not fill the gap with an assumption.
