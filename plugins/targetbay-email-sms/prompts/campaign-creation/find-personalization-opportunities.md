---
title: Personalise Beyond First Name
summary: Which parts of my messages should vary per recipient, driven only by data I actually have.
skill: dynamic-content-personalizer
---
Using TargetBay Email & SMS (`dynamic-content-personalizer` skill), find where personalisation would make [CAMPAIGN OR AUTOMATION NAME] more relevant.

Derive from my data which customer, order and product signals are populated and fresh enough to trust.

For each variable element give:
- the signal driving it and how reliable it is
- the fallback when the signal is missing, and the share of recipients who'd get it
- how stale the signal can get before it's wrong
- expected relevance gain versus effort

Also list elements you rejected, ones dropped because the fallback is as good, fields I'd need but don't have, and a preview of a populated and an empty version.

If data is missing, say so and ask; don't estimate.
