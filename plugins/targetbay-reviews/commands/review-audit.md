---
description: Assess the whole review and UGC programme and rank what is most worth fixing first
---

Use the `review-program-audit` skill.

Nobody has named the problem, so do not assume one and do not default to collecting more reviews. Assess
collection, rating health, display, media, moderation and distribution, then name the binding constraint
before ranking anything.

Report the dimensions that are healthy as well as the ones that are not, and route every finding to the
skill that owns it.

Analysis and recommendation only — no requests, no replies, no sends (`rules/safety-rules.md`).
